import { AppShell } from "../components/layout/AppShell";

export default function Administracion() {
  return (
    <AppShell title="Administracion">
      <p style={{ color: "var(--muted)" }}>
        Pantalla en construcción — siguiente paso del frontend.
      </p>
    </AppShell>
  );
}
